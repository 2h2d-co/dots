# dots example config
# Default path: ${XDG_CONFIG_HOME:-$HOME/.config}/dots/config.toml

default_profile = "personal"
# pager = "hunk patch --pager -"

[profiles]
personal = "~/dotfiles"
work = "~/work-dotfiles"
